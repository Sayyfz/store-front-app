{
    "version": 2,
    "builds": [
        {
            "src": "backend/src/**/*.ts",
            "use": "@vercel/node",
            "config": {
                "maxLambdaSize": "15mb"
            }
        }
    ],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "backend/dist/index.js"
        }
    ]
}
